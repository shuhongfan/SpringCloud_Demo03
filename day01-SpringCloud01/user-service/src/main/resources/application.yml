server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://localhost:3307/cloud-user?useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver

#      discovery:
#        cluster-name: WH
mybatis:
  type-aliases-package: cn.itcast.user.pojo
  configuration:
    map-underscore-to-camel-case: true
logging:
  level:
    cn.itcast: debug
  pattern:
    dateformat: MM-dd HH:mm:ss:SSS
#eureka:
#  client:
#    service-url:
#      defaultZone: http://127.0.0.1:10086/eureka
pattern:
  name: 本地环境local